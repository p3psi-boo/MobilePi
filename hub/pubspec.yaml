name: mobilepi_hub
description: MobilePi 中枢转发服务器 - WebSocket 透明路由与 Node 更新包宿主
publish_to: 'none'

version: 0.1.0

environment:
  sdk: ^3.11.4

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_web_socket: ^2.0.0
  web_socket_channel: ^2.4.0
  args: ^2.5.0
  logging: ^1.2.0
  crypto: ^3.0.0
  path: ^1.9.0

  # 共享协议与模型
  mobilepi_shared:
    path: ../shared

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6