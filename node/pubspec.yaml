name: mobilepi_node
description: MobilePi 物理机守护节点 - Agent 封装、会话管理与自动更新
publish_to: 'none'

version: 0.1.0

environment:
  sdk: ^3.11.4

dependencies:
  shelf: ^1.4.0
  http: ^1.2.0
  args: ^2.5.0
  logging: ^1.2.0
  path: ^1.9.0
  web_socket_channel: ^2.4.0
  crypto: ^3.0.0
  uuid: ^4.4.0
  sqlite3: ^2.4.0
  process_run: ^0.14.0

  # 共享协议与模型
  mobilepi_shared:
    path: ../shared

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  daemon: node